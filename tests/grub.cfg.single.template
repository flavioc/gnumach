set timeout=0
set default=0

menuentry "gnumach test" {
    multiboot /boot/gnumach console=com0
    module /boot/BOOTMODULE BOOTMODULE m1 '${host-port}' '${device-port}' '$(task-create)' '$(task-resume)'
    boot
}
